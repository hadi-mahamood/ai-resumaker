# Dependency directories
node_modules/

# Local env files
.env
.env.local

# Server database files
profiles_db.json

# Lockfiles (optional, but keep package-lock.json out if we want clean simple git)
# package-lock.json
